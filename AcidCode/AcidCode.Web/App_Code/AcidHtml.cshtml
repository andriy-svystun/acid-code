@helper MenuItem(string title, string url, bool active)
{
    <li class="@string.Concat("nav-item", active ? " active" : "")" >
        <a class="nav-link" href="@url">
            @title @if (active)
            {<span class="sr-only">(current)</span>}
        </a>
    </li>
}